# CLAUDE.md

> `[要確認]` は未確定事項です。判明次第このドキュメントを更新してください。

## プロジェクト概要

- **プロジェクト名**: label-maker
- **概要**: テキストを入力し、ラベル用紙に合わせたPDFを生成するWebアプリケーション。
- **対象ユーザー**: 個人利用
- **リポジトリ**: https://github.com/yNagata6158/label-maker (Private)

## 技術仕様

- **構成**: サーバーサイドは持たない。ブラウザのみで完結するクライアントサイドアプリケーション。
- **フレームワーク**: React 19 + Vite
- **言語**: TypeScript
- **パッケージ管理**: npm
- **Lint**: oxlint（`.oxlintrc.json` に設定。`react`, `typescript`, `oxc` プラグイン使用）
- **Node.jsバージョン**: v24系を想定（開発環境準拠）
- **主要スクリプト**
  - `npm run dev`: 開発サーバー起動
  - `npm run build`: 型チェック（`tsc -b`）＋ 本番ビルド
  - `npm run lint`: Lint実行
  - `npm run preview`: ビルド成果物のプレビュー
- **ディレクトリ構成**（現状）
  ```
  label-maker/
  ├── public/          # 静的アセット
  ├── src/
  │   ├── assets/      # 画像等
  │   ├── App.tsx       # ルートコンポーネント
  │   ├── main.tsx       # エントリーポイント
  │   └── index.css / App.css
  ├── index.html
  ├── tsconfig.json / tsconfig.app.json / tsconfig.node.json
  ├── vite.config.ts
  └── package.json
  ```
- **状態管理**: React標準のstate/propsで対応する（サーバー連携がなく規模も小さいため、外部状態管理ライブラリは現時点で不要と判断）。
- **印刷・出力方式**: クライアントサイドのPDF生成ライブラリ（例: jsPDF, pdf-lib等）を使用してPDFを生成し、ブラウザの印刷機能から印刷できるようにする。 [要確認: 使用する具体的なライブラリ]

## ラベルの仕様

- **ラベルの種類**: 宛名ラベル
- **用紙サイズ**: A4（210mm × 297mm）固定
- **ページ設定**
  - 用紙の余白（上下左右）をユーザーが設定できる
  - ラベル1枚のサイズ（幅×高さ）をユーザーが設定できる
  - ラベル間の余白をユーザーが設定できる
  - 上記の設定値から、A4用紙内に何枚のラベルを配置できるかを自動計算するグリッドレイアウトとする（自由配置・個別編集は行わない）
- **入力データ**
  - 手入力を必須機能とする（氏名・住所・会社名を入力するフォーム）
  - 複雑にならない範囲でCSVインポート機能を追加する [要確認: CSVの項目フォーマット（列の並び等）]
- **入力項目**: 氏名、住所、会社名
- **印字内容**: 入力したテキストを、同一シート内の全ラベルに同じ内容で印字する（宛先ごとに異なる内容を印字する機能は現時点では対象外）
- **出力形式**: PDFをクライアントサイドで生成し、ブラウザの印刷機能で印刷する
- **保存・再利用**: サーバー保存は行わない（サーバーを持たない構成のため）。ブラウザ内保存（localStorage等）による入力内容の一時保持を行うかは未定。 [要確認]

## コーディング規約

- **コンポーネント**
  - 1ファイル1コンポーネントを基本とする。
  - コンポーネント名は `PascalCase`、ファイル名もコンポーネント名に合わせる（例: `LabelEditor.tsx`）。
  - 関数コンポーネント + Hooksを使用し、クラスコンポーネントは使用しない。
  - Props・状態には可能な限り明示的な型を付ける（`any` は原則使用しない）。
- **命名規則**
  - 変数・関数名は `camelCase`。
  - 型・インターフェースは `PascalCase`。
  - 定数は `UPPER_SNAKE_CASE`。
  - CSSクラス名は `kebab-case`。
- **スタイリング**: コンポーネント単位でCSSファイルを分割する現行方針を踏襲する。 [要確認: CSS Modules 等の導入要否]
- **Lint/型チェック**
  - コミット前に `npm run lint` と `npm run build`（型チェック含む）を実行し、エラー・警告を解消する。
  - フォーマッタ（Prettier等）は未導入。導入する場合は別途合意する。 [要確認]
- **コメント**: 自明でないロジック・仕様上の理由がある箇所にのみ日本語または英語でコメントを残す（コードから読み取れる内容の重複コメントは避ける）。
- **不要コード**: 使われなくなったコード・console.logはコミット前に削除する。

## 作業ルール

- **ブランチ運用**: `main` を保護ブランチとし、作業は機能ごとにブランチを切って行う。 [要確認: ブランチ命名規則（例: `feature/xxx`, `fix/xxx`）]
- **コミット**
  - 1コミット1論理変更を意識する。
  - コミットメッセージは変更内容が分かる形で記述する（日本語可）。
- **プルリクエスト**: 実装がある程度まとまった単位でPRを作成し、マージ前にレビューを受ける。 [要確認: レビュー必須者、セルフマージ可否]
- **テスト**: 現時点でテストフレームワーク未導入。主要機能実装時にVitest等の導入を検討する。 [要確認]
- **Claude Codeとの作業の進め方**
  - 仕様や設計判断で不明点がある場合は、実装前に確認する。
  - 大きな変更やリポジトリ構成に影響する変更（依存追加、初期化系コマンド等）は事前に確認を取ってから実行する。
  - 破壊的な操作（`git push --force`、ファイル削除等）は明示的な指示がない限り行わない。
