# CLAUDE.md

## プロジェクト概要

- **プロジェクト名**: label-maker
- **概要**: テキストを入力し、ラベル用紙に合わせたPDFを生成するWebアプリケーション。
- **対象ユーザー**: 個人利用
- **リポジトリ**: https://github.com/yNagata6158/label-maker (Private)
- **対応ブラウザ**: Chromeのみを公式サポート対象とする。
- **公開・デプロイ**: 現時点ではデプロイは行わず、ローカル（`npm run dev`）で実行して使用する。

## 技術仕様

- **構成**: サーバーサイドは持たない。ブラウザのみで完結するクライアントサイドアプリケーション。
- **フレームワーク**: React 19 + Vite
- **言語**: TypeScript
- **パッケージ管理**: npm
- **PDF生成**: jsPDF を使用してクライアントサイドでPDFを生成する。日本語テキストを描画するため、日本語対応フォント（TTF等）をbase64化してjsPDFに埋め込む。
- **状態の永続化**: 入力内容（氏名・郵便番号・住所・会社名、および用紙設定）をlocalStorageに保存し、ページを再読み込みしても復元できるようにする。
- **Lint**: oxlint（`.oxlintrc.json` に設定。`react`, `typescript`, `oxc` プラグイン使用）
- **フォーマッタ**: Prettierを導入する（未導入。実装着手時に設定ファイル・依存関係を追加する）。
- **テスト**: 現時点では導入しない。個人利用の小規模アプリのため、まず実装を優先する。将来的にグリッド配置計算など影響範囲が大きいロジックにはVitestでのユニットテスト導入を検討する。
- **Node.jsバージョン**: v24系を想定（開発環境準拠）
- **主要スクリプト**
  - `npm run dev`: 開発サーバー起動
  - `npm run build`: 型チェック（`tsc -b`）＋ 本番ビルド
  - `npm run lint`: Lint実行
  - `npm run preview`: ビルド成果物のプレビュー
- **ディレクトリ構成**（現状）
  ```
  label-maker/
  ├── public/          # 静的アセット
  ├── src/
  │   ├── assets/      # 画像等
  │   ├── App.tsx       # ルートコンポーネント
  │   ├── main.tsx       # エントリーポイント
  │   └── index.css / App.css
  ├── index.html
  ├── tsconfig.json / tsconfig.app.json / tsconfig.node.json
  ├── vite.config.ts
  └── package.json
  ```
- **状態管理**: React標準のstate/propsで対応する（サーバー連携がなく規模も小さいため、外部状態管理ライブラリは不要と判断）。
- **スタイリング**: コンポーネント単位でCSSファイルを分割する現行方針（プレーンCSS）を継続する。CSS Modules等は導入しない。

## ラベルの仕様

- **ラベルの種類**: 宛名ラベル
- **入力項目**
  - 氏名（必須）
  - 郵便番号（必須）
  - 住所（必須）
  - 会社名（任意）
- **入力バリデーション**: 必須項目（氏名・郵便番号・住所）が未入力の場合はPDF生成をブロックする。郵便番号の桁数チェックなど形式面のバリデーションは行わない。
- **印字内容**: 入力したテキストを、同一シート内の全ラベルに同じ内容で印字する（宛先ごとに異なる内容を印字する機能は現時点では対象外）。
- **CSVインポート**: 手入力に加えて、CSVインポート機能を提供する（複雑にならない範囲で実装）。
  - 列順: `氏名, 郵便番号, 住所, 会社名`（ヘッダー行あり想定）
  - 位置づけ: 手入力の代わりにCSVから1件分のデータを読み込み、入力フォームに反映する機能とする（CSVの複数行を宛先ごとに別シートへ展開する、いわゆるメールマージ的な一括印刷機能は対象外）。フォームに反映された後は、他の入力項目と同様に全ラベルへ同一内容が印字される。
- **用紙・レイアウト設定**
  - 用紙サイズ: A4（210mm × 297mm）固定
  - 用紙の余白（上下左右）をユーザーが設定できる
  - ラベル1枚のサイズ（幅×高さ）をユーザーが設定できる
  - ラベル間の余白をユーザーが設定できる
  - 上記の設定値から、A4用紙内に配置できるラベルの行数・列数を自動計算するグリッドレイアウトとする（自由配置・ラベルごとの個別編集は行わない）
  - 計算上ぴったり収まらず余りが出る場合は、余白を均等に振り分けてラベル群をシート中央に配置する
- **フォント・文字サイズ**: フォント種類は固定（埋め込む日本語フォント1種類）。文字サイズはユーザーが調整できる。
- **プレビュー**: PDF生成前に、用紙設定・ラベル配置を画面上でリアルタイムに確認できるプレビュー機能を用意する。
- **出力形式**: PDFをクライアントサイドで生成し、ブラウザの印刷機能で印刷する。
- **保存・再利用**: サーバー保存は行わない。ブラウザのlocalStorageに入力内容・用紙設定を保存し、再訪問時に復元する。

## コーディング規約

- **コンポーネント**
  - 1ファイル1コンポーネントを基本とする。
  - コンポーネント名は `PascalCase`、ファイル名もコンポーネント名に合わせる（例: `LabelEditor.tsx`）。
  - 関数コンポーネント + Hooksを使用し、クラスコンポーネントは使用しない。
  - Props・状態には可能な限り明示的な型を付ける（`any` は原則使用しない）。
- **命名規則**
  - 変数・関数名は `camelCase`。
  - 型・インターフェースは `PascalCase`。
  - 定数は `UPPER_SNAKE_CASE`。
  - CSSクラス名は `kebab-case`。
- **フォーマット**: Prettierの自動整形に従う（保存時整形を基本とする）。
- **Lint/型チェック**
  - コミット前に `npm run lint` と `npm run build`（型チェック含む）を実行し、エラー・警告を解消する。
- **コメント**: 自明でないロジック・仕様上の理由がある箇所にのみ日本語または英語でコメントを残す（コードから読み取れる内容の重複コメントは避ける）。
- **不要コード**: 使われなくなったコード・console.logはコミット前に削除する。

## 作業ルール

- **ブランチ運用**: 個人開発のため、ブランチ・PRは作成せず `main` に直接コミットして進める。
- **コミット**
  - 1コミット1論理変更を意識する。
  - コミットメッセージは変更内容が分かる形で記述する（日本語可）。
- **Claude Codeとの作業の進め方**
  - 仕様や設計判断で不明点がある場合は、実装前に確認する。
  - 大きな変更やリポジトリ構成に影響する変更（依存追加、初期化系コマンド等）は事前に確認を取ってから実行する。
  - 破壊的な操作（`git push --force`、ファイル削除等）は明示的な指示がない限り行わない。
